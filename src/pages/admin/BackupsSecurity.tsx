import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function BackupsSecurity() {
    return (
        <div className="space-y-6">
            <Card>
                <CardHeader>
                    <CardTitle>Backups & Security</CardTitle>
                </CardHeader>
                <CardContent>
                    <p>Manage backups and security settings here.</p>
                </CardContent>
            </Card>
        </div>
    );
}
